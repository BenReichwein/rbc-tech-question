import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
// Rendering the App.js file here
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);